# Historical Summary

This note describes an earlier exploratory attempt. It is superseded by the
repo-root `solution.py`, which is the active submission artifact.

The active root solver is:
- Python-only,
- deterministic,
- verifier-backed on its supported domain,
- explicit about unsupported inputs instead of falling back to random search or
  generated C code.

The mixed-language and stochastic components mentioned below are historical
research artifacts inside this archive only. They are not part of the live
submission path and should not be evaluated as the final answer.
